<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>NutriKids - Cardápios</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>

<header>
  <h1>🍏 NutriKids</h1>
  <nav>
    <a href="/">Início</a>
    <a href="/cardapio">Cardápios</a>
  </nav>
</header>

<main>

  <section>
    <h2>Cadastro de Cardápio</h2>

    <% if (typeof error !== "undefined") { %>
      <div class="alert"><%= error %></div>
    <% } %>

    <form action="/cardapio/novo" method="POST">
      <label>Nome</label>
      <input type="text" name="nome" required />

      <label>Descrição</label>
      <input type="text" name="descricao" required />

      <label>Calorias</label>
      <input type="number" name="calorias" required />

      <label>Data</label>
      <input type="date" name="data_cardapio" required />

      <button type="submit">Salvar Cardápio</button>
    </form>
  </section>

  <section style="margin-top: 40px;">
    <h3>Cardápios Cadastrados</h3>

    <table>
      <tr>
        <th>Nome</th>
        <th>Descrição</th>
        <th>Calorias</th>
        <th>Data</th>
      </tr>

      <% cardapios.forEach(c => { %>
        <tr>
          <td><%= c.nome %></td>
          <td><%= c.descricao %></td>
          <td><%= c.calorias %></td>
          <td><%= new Date(c.data).toLocaleDateString("pt-BR") %></td>
        </tr>
      <% }) %>

    </table>
  </section>

</main>

<footer>
  <p>&copy; <%= new Date().getFullYear() %> NutriKids</p>
</footer>

</body>
</html>
